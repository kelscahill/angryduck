{% if options.alert_text %}
  <div class="c-alert js-alert">
    <div class="c-alert__content">
      <p>
        {{ options.alert_text }}
        {% if options.alert_link %}
          {% include "00-atoms/buttons/link.twig"
            with {
              link: {
                url: options.alert_link.url,
                text: options.alert_link.title,
                target: options.alert_link.target
              }
            }
          %}
        </p>
      {% endif %}
    </div>
    <button aria-role="close" class="c-alert__close o-icon u-icon--small js-alert-close">
      <span class="o-icon u-icon--s">{% include "00-atoms/icons/close.twig" %}</span>
    </button>
  </div>
{% endif %}